What happens when you initialize a repository? Why do you need to do it?

When initlizing a repository the necessary metadata. The metadata is necessary to maintaing the repository's multiple versions that will be created in the near future